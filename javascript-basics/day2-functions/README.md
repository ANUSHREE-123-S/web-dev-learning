# Day 2 - Functions

## Functions
- Reusable block of code
- Uses return to send value

Example:
function add(a, b) {
  return a + b;
}

## Arrow Functions
- Short syntax
- Used in React

Example:
const add = (a, b) => a + b;

## Mistakes
- Forgetting return
- Using {} without return